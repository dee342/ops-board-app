'use strict';

angular
    .module('OpsBoard')
    .controller(
        'AddVolunteerCounts',
        function($scope, $filter, $modal, $log, OpsBoardRepository, ClientSideError, groups) {
           
          
        })